import React from 'react'
import {Helmet} from "react-helmet-async"

const Title = ({title = "Chat", description = "this is the chat app called Chatloop"}) => {
  return (
    <div>
      
    </div>
  )
}

export default Title
