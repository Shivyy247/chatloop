import { Link } from "react-router-dom"


const Chatitem = ({
    avatar = [],
    name,
    _id,
    groupChat = false,
    sameSender,
    isOnline,
    newMessage,
    index = 0,
    handleDeleteChatOpen,

}) => {
  return (
      <Link to={"/"}>
          
      </Link>
  )
}

export default Chatitem
